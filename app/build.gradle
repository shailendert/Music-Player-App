apply plugin: 'com.android.application'
android {
    signingConfigs {
        config {
            keyAlias 'musickey'
            keyPassword 'st2410'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "25.0.2"
    defaultConfig {
        applicationId "com.st.musicplayer"
        minSdkVersion 16
        targetSdkVersion 18
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}

dependencies {
}